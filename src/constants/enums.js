const ROLES = {
  ADMIN: "admin",
  STUDENT: "student",
  TEACHER: "teacher"
};

const QUESTION_TYPES = {
  SINGLE_CORRECT: "single_correct",
  MULTI_CORRECT: "multi_correct"
};

const MARKING_TYPES = {
  NEGATIVE_2_5: -2.5,
  NEGATIVE_0_5: -0.5,
  ZERO: 0
};

module.exports = {
  ROLES,
  QUESTION_TYPES,
  MARKING_TYPES
};
